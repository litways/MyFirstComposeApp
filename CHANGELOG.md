# Changelog

## [0.4.4] - 2026-02-05 15:13:45
### 本次 Codex 指令
```text
下发Codex任务内容


【本次修改内容】

一、作用页面 / 模块：
* 底部导航栏中的“记录”按钮（圆形加号按钮）
* 相关 Kotlin 文件：HomeScaffold.kt（底部导航 NavigationBarItem 的 label 参数）

二、问题现状说明： 1）我希望移除底部导航栏中“记录”按钮下方的文字标签（label），但当前实现方式导致编译失败； 2）当前 HomeScaffold.kt 中对 NavigationBarItem 的 label 做了条件判断： label = if (isRecord) null else { Text(...) } 3）运行时出现编译错误（示例）：
* Argument type mismatch: actual type is 'kotlin.Unit?' but '@Composable () -> Unit' was expected
* @Composable invocations can only happen from the context of a @Composable function 4）原因是：NavigationBarItem 的 label 参数类型为 (@Composable () -> Unit)?，else 分支必须返回一个 Composable lambda，而不是直接执行的代码块（Unit）。

三、期望行为： 1）仅对“记录”（Routes.CREATE / isRecord 为 true）这一项移除 label：label 必须为 null； 2）其他入口（总览、待办、追溯、用户）的 label 必须保持原样显示、且不受影响； 3）圆形背景区域的尺寸、颜色映射（正色 / 反色）及居中位置保持不变； 4）“记录”按钮的点击行为与导航逻辑保持不变； 5）修复上述编译错误，确保项目可正常编译运行。

四、边界与限制： 1）仅允许修复 label 的实现方式（确保类型正确），不得调整圆形背景、图标大小或底部导航栏布局； 2）不得影响其他底部导航入口（总览、待办、追溯、用户）的文字或样式； 3）不得影响底部导航栏的高度、对齐方式或点击区域； 4）不得引入新的交互行为、动画或视觉效果； 5）不得新增第三方依赖； 6）除 HomeScaffold.kt 与 CHANGELOG.md 外，不得修改任何其他文件。

五、修改类型：
* 问题修复（编译错误修复 + UI label 行为修复）
在修改代码前，先告诉我：你将修改的 Kotlin 文件路径是哪一个，以及你将修复的 label/Text 具体是哪一行/哪个参数（例如：NavigationBarItem 的 label 参数）。


在修改代码前，先告诉我：你将修改的 Kotlin 文件路径是哪一个，以及你将删除的 Text/label 具体是哪一行/哪个参数。


【需遵循规范】
一、通用执行规范 V1
二、Material Design 3 组件与样式规范 V1
三、版本更新规范 V1
———————————————————————————————————————————————————————
【通用执行规范 V1】
以下规范对本次及后续所有 Codex 修改任务生效，必须严格遵守：
一、工程边界
1）不要调整目录结构；
2）不要调整导航或页面结构；
3）不要重命名现有函数或变量；
4）不要移动文件；
5）不要引入新的第三方依赖；
6）不要影响其他页面的返回逻辑或既有行为。
二、修改范围
1）只允许修改本次任务明确指定的文件/页面/功能；
2）不要修改未明确允许的页面或模块；
3）如需新增状态，请使用页面内的最小本地状态；
4）不要擅自抽取公共组件或新增公共 UI 层。
三、输出要求
1）只输出最终代码，不要包含解释说明；
2）如修改文件，必须输出该文件的完整内容；
3）不要输出 diff 或片段代码。
四、质量与验证
1）修改后项目必须可以正常编译；
2）不允许新增编译错误或 warning；
3）修改完成后请执行一次构建验证（如 assembleDebug）；
4）不要口头声明“已测试”；
5）如无法执行测试，请明确说明原因，并至少保证项目可编译通过。
五、执行原则
1）如存在多种实现方式，请优先选择最小改动方案；
2）不得因实现便利而破坏既有工程结构或规范。
———————————————————————————————————————————————————————
【版本更新规范 V1】
以下规范用于约束版本号管理与 CHANGELOG.md 的维护方式，必须严格遵守：
一、CHANGELOG 文件规则
1）项目根目录下使用 CHANGELOG.md 作为唯一版本更新日志文件；
2）如果 CHANGELOG.md 不存在，必须新建；
3）CHANGELOG.md 仅用于记录版本更新，不承担其他说明职责。
4）更新 CHANGELOG.md 时，只允许在文件顶部追加新版本块，不得触碰历史内容（包括空格/换行）
5）每次更新时读取历史版本记录，在历史基础上更新。
二、版本记录新增规则
1）每次 Codex 完成一次有效修改任务后，必须更新 CHANGELOG.md；
2）新版本记录必须追加在文件顶部；
3）禁止修改、删除或重排历史版本记录。
三、版本号规则
1）版本号格式统一为：0.x.y；
2）x 表示功能版本号，y 表示修订版本号；
3）当本次修改属于新增功能或功能增强时：递增 x，y 重置为 0；
4）当本次修改属于问题修复、小改动或非功能性调整时：x 保持不变，仅递增 y；
5）版本号必须连续，不得跳号或回退。
四、版本记录内容规范
每个版本记录必须完整包含以下内容：
1）版本号（使用中括号包裹）；
2）修改日期，格式为 YYYY-MM-DD hh:nn:ss
3）本次使用的完整 Codex 提示语/指令原文，必须原样写入，不得删减、概括或改写。
五、版本记录示例
# Changelog
## [版本号] - YYYY-MM-DD hh:nn:ss
### 本次 Codex 指令
```text
（完整提示语/指令原文）

———————————————————————————————————————————————————————
【UI组件与样式规范 V1】
以下规范用于约束本工程中所有 UI 的实现方式，基于 Material Design 3，必须严格遵守：
一、总体原则
1）所有 UI 必须基于 Material Design 3 组件体系实现；
2）优先使用 Material 3 提供的现有组件，而非自定义实现；
3）保持整体 UI 风格一致、克制、可维护。
二、组件使用规范
1）优先使用 Material 3 官方组件（如 Button、Text、Card、AlertDialog 等）；
2）禁止自行实现与 Material 3 已有组件功能重复的自定义组件；
3）如已有 Material 3 组件可满足需求，不得自定义替代；
4）避免使用过时或非 Material 3 的组件风格。
三、颜色与主题规范
1）所有颜色应来自 MaterialTheme.colorScheme；
2）禁止在业务代码中写死颜色值（如 Color(0xFFxxxxxx)）；
3）禁止新增非必要的颜色常量；
4）背景色、文字色应使用主题自动适配颜色（如 primary、onPrimary 等）。
四、字体与排版规范
1）文本样式应使用 MaterialTheme.typography 中的现有样式；
2）禁止在业务代码中自定义 TextStyle；
3）禁止手动指定字体大小、字体族或字重；
4）文本层级应通过选择合适的 typography 样式体现。
五、布局与间距规范
1）布局应以简单、清晰为原则；
2）避免过深的嵌套层级；
3）间距通过 Modifier.padding 实现；
4）禁止通过写死高度或宽度制造“视觉间距”。
六、状态与反馈规范
1）交互状态（enabled / disabled / loading 等）应使用 Material 3 组件自带能力；
2）避免自行实现状态视觉效果；
3）错误、提示等反馈优先使用 Material 3 组件（如 Snackbar、Dialog）。
七、风格一致性规范
1）同类页面应使用一致的组件与样式选择；
2）不得在不同页面中引入明显不同的 UI 风格；
3）如存在多种实现方式，应选择与现有页面风格最一致的方案。
八、工程实现约束
1）不得因 UI 实现需要而修改主题结构；
2）不得为 UI 目的引入新的依赖；
3）UI 实现应服务于业务清晰度，而非展示技巧。

```

## [0.4.3] - 2026-02-05
### 本次 Codex 指令
```text
### 本次 Codex 指令
```text
下发Codex任务内容


【本次修改内容】

一、作用页面 / 模块：
- 底部导航栏中的“记录”按钮（圆形加号按钮）

二、问题现状说明：
1）当前底部导航栏中的“记录”按钮，
   在圆形加号图标下方仍显示“记录”文字；
2）在已通过圆形背景区域与加号图标进行视觉强化的情况下，
   文本标签显得冗余，且对底部导航栏整体简洁性造成干扰。

三、期望行为：
1）移除底部导航栏中“记录”按钮下方的文字标签“记录”；
2）保留圆形背景区域与加号图标作为该入口的唯一视觉表达；
3）圆形背景区域的尺寸、颜色映射（正色 / 反色）及居中位置保持不变；
4）“记录”按钮的点击行为与导航逻辑保持不变。

四、边界与限制：
1）仅移除文字标签，不得调整圆形背景、图标大小或底部导航栏布局；
2）不得影响其他底部导航入口（总览、待办、追溯、用户）的文字或样式；
3）不得影响底部导航栏的高度、对齐方式或点击区域；
4）不得引入新的交互行为、动画或视觉效果。

五、修改类型：
- 问题修复


在修改代码前，先告诉我：你将修改的 Kotlin 文件路径是哪一个，以及你将删除的 Text/label 具体是哪一行/哪个参数。


【需遵循规范】
一、通用执行规范 V1
二、Material Design 3 组件与样式规范 V1
三、版本更新规范 V1
———————————————————————————————————————————————————————
【通用执行规范 V1】
以下规范对本次及后续所有 Codex 修改任务生效，必须严格遵守：
一、工程边界
1）不要调整目录结构；
2）不要调整导航或页面结构；
3）不要重命名现有函数或变量；
4）不要移动文件；
5）不要引入新的第三方依赖；
6）不要影响其他页面的返回逻辑或既有行为。
二、修改范围
1）只允许修改本次任务明确指定的文件/页面/功能；
2）不要修改未明确允许的页面或模块；
3）如需新增状态，请使用页面内的最小本地状态；
4）不要擅自抽取公共组件或新增公共 UI 层。
三、输出要求
1）只输出最终代码，不要包含解释说明；
2）如修改文件，必须输出该文件的完整内容；
3）不要输出 diff 或片段代码。
四、质量与验证
1）修改后项目必须可以正常编译；
2）不允许新增编译错误或 warning；
3）修改完成后请执行一次构建验证（如 assembleDebug）；
4）不要口头声明“已测试”；
5）如无法执行测试，请明确说明原因，并至少保证项目可编译通过。
五、执行原则
1）如存在多种实现方式，请优先选择最小改动方案；
2）不得因实现便利而破坏既有工程结构或规范。
———————————————————————————————————————————————————————
【版本更新规范 V1】
以下规范用于约束版本号管理与 CHANGELOG.md 的维护方式，必须严格遵守：
一、CHANGELOG 文件规则
1）项目根目录下使用 CHANGELOG.md 作为唯一版本更新日志文件；
2）如果 CHANGELOG.md 不存在，必须新建；
3）CHANGELOG.md 仅用于记录版本更新，不承担其他说明职责。
4）更新 CHANGELOG.md 时，只允许在文件顶部追加新版本块，不得触碰历史内容（包括空格/换行）
5）每次更新时读取历史版本记录，在历史基础上更新。
二、版本记录新增规则
1）每次 Codex 完成一次有效修改任务后，必须更新 CHANGELOG.md；
2）新版本记录必须追加在文件顶部；
3）禁止修改、删除或重排历史版本记录。
三、版本号规则
1）版本号格式统一为：0.x.y；
2）x 表示功能版本号，y 表示修订版本号；
3）当本次修改属于新增功能或功能增强时：递增 x，y 重置为 0；
4）当本次修改属于问题修复、小改动或非功能性调整时：x 保持不变，仅递增 y；
5）版本号必须连续，不得跳号或回退。
四、版本记录内容规范
每个版本记录必须完整包含以下内容：
1）版本号（使用中括号包裹）；
2）修改日期，格式为 YYYY-MM-DD；
3）本次使用的完整 Codex 提示语/指令原文，必须原样写入，不得删减、概括或改写。
五、版本记录示例
# Changelog
## [版本号] - YYYY-MM-DD
### 本次 Codex 指令
```text
（完整提示语/指令原文）

———————————————————————————————————————————————————————
【UI组件与样式规范 V1】
以下规范用于约束本工程中所有 UI 的实现方式，基于 Material Design 3，必须严格遵守：
一、总体原则
1）所有 UI 必须基于 Material Design 3 组件体系实现；
2）优先使用 Material 3 提供的现有组件，而非自定义实现；
3）保持整体 UI 风格一致、克制、可维护。
二、组件使用规范
1）优先使用 Material 3 官方组件（如 Button、Text、Card、AlertDialog 等）；
2）禁止自行实现与 Material 3 已有组件功能重复的自定义组件；
3）如已有 Material 3 组件可满足需求，不得自定义替代；
4）避免使用过时或非 Material 3 的组件风格。
三、颜色与主题规范
1）所有颜色应来自 MaterialTheme.colorScheme；
2）禁止在业务代码中写死颜色值（如 Color(0xFFxxxxxx)）；
3）禁止新增非必要的颜色常量；
4）背景色、文字色应使用主题自动适配颜色（如 primary、onPrimary 等）。
四、字体与排版规范
1）文本样式应使用 MaterialTheme.typography 中的现有样式；
2）禁止在业务代码中自定义 TextStyle；
3）禁止手动指定字体大小、字体族或字重；
4）文本层级应通过选择合适的 typography 样式体现。
五、布局与间距规范
1）布局应以简单、清晰为原则；
2）避免过深的嵌套层级；
3）间距通过 Modifier.padding 实现；
4）禁止通过写死高度或宽度制造“视觉间距”。
六、状态与反馈规范
1）交互状态（enabled / disabled / loading 等）应使用 Material 3 组件自带能力；
2）避免自行实现状态视觉效果；
3）错误、提示等反馈优先使用 Material 3 组件（如 Snackbar、Dialog）。
七、风格一致性规范
1）同类页面应使用一致的组件与样式选择；
2）不得在不同页面中引入明显不同的 UI 风格；
3）如存在多种实现方式，应选择与现有页面风格最一致的方案。
八、工程实现约束
1）不得因 UI 实现需要而修改主题结构；
2）不得为 UI 目的引入新的依赖；
3）UI 实现应服务于业务清晰度，而非展示技巧。

请在 HomeScaffold.kt 中仅修改 “记录/CREATE（Routes.CREATE）” 这一项的文字标签：

当前 NavigationBarItem 在 items.forEach 中统一设置了：
label = { Text(stringResource(item.labelRes)) }

要求：
1) 仅对 isRecord (item.route == Routes.CREATE) 这一项移除 label：把 label 设为 null；
2) 其他入口（总览/待办/追溯/用户）label 保持不变；
3) 圆形背景、图标大小、导航栏布局、点击逻辑全部不变；
4) 除 HomeScaffold.kt 和 CHANGELOG.md 外，不得修改其他文件。

实现建议：将 label 改为
label = if (isRecord) null else { Text(stringResource(item.labelRes)) }

修改完成后输出 HomeScaffold.kt 的完整内容，并按版本更新规范更新 CHANGELOG.md（只在顶部追加，不触碰历史记录）。
```

## [0.4.2] - 2026-01-27
### 本次 Codex 指令
```text
下发Codex任务内容


【本次修改内容】

一、作用页面 / 模块：
- 底部导航栏中的“记录”按钮
- 记录按钮对应的记录页面（新增变化点页面）

二、问题现状说明：
1）当前“记录”按钮已采用固定的圆形背景区域进行凸显；
2）但圆形背景区域尺寸偏小，与底部导航栏整体比例接近，
   尚不足以形成“底栏主入口”的视觉锚点；
3）在未选中状态下，尽管已使用正色 / 反色区分，
   但整体突出程度仍有提升空间。

三、期望行为：
1）在保持现有圆形背景结构的前提下，
   适当增大“记录”按钮的圆形背景区域尺寸，
   使其在底部导航栏中形成明显的视觉焦点；
2）圆形背景区域需明显大于其他导航项的图标区域，
   但不得超出底部导航栏的可点击高度范围；
3）圆形背景区域始终存在于“记录”按钮外层，
   不随选中状态消失；
4）“记录”按钮的选中与未选中状态，
   仅通过圆形背景区域与图标的正色 / 反色进行区分：
   - 选中状态：
     - 圆形背景使用 MaterialTheme.colorScheme.primary；
     - 图标使用 MaterialTheme.colorScheme.onPrimary；
   - 未选中状态：
     - 圆形背景使用 MaterialTheme.colorScheme.onPrimary；
     - 图标使用 MaterialTheme.colorScheme.primary；
5）不得通过缩放、动画、阴影或透明度（alpha）变化
   来制造突出效果。

四、边界与限制：
1）不得改变底部导航栏的整体布局结构、排列方式或高度；
2）不得改变“记录”按钮及其他导航按钮的点击行为与导航逻辑；
3）不得影响其他底部导航入口（总览、待办、追溯、用户）的显示或交互；
4）不得引入新的颜色定义、主题结构或第三方依赖；
5）本次修改仅限于“记录”按钮的尺寸与颜色映射调整，
   不涉及页面内容或业务逻辑改动。

五、修改类型：
- 问题修复


【需遵循规范】
一、通用执行规范 V1
二、Material Design 3 组件与样式规范 V1
三、版本更新规范 V1
———————————————————————————————————————————————————————
【通用执行规范 V1】
以下规范对本次及后续所有 Codex 修改任务生效，必须严格遵守：
一、工程边界
1）不要调整目录结构；
2）不要调整导航或页面结构；
3）不要重命名现有函数或变量；
4）不要移动文件；
5）不要引入新的第三方依赖；
6）不要影响其他页面的返回逻辑或既有行为。
二、修改范围
1）只允许修改本次任务明确指定的文件/页面/功能；
2）不要修改未明确允许的页面或模块；
3）如需新增状态，请使用页面内的最小本地状态；
4）不要擅自抽取公共组件或新增公共 UI 层。
三、输出要求
1）只输出最终代码，不要包含解释说明；
2）如修改文件，必须输出该文件的完整内容；
3）不要输出 diff 或片段代码。
四、质量与验证
1）修改后项目必须可以正常编译；
2）不允许新增编译错误或 warning；
3）修改完成后请执行一次构建验证（如 assembleDebug）；
4）不要口头声明“已测试”；
5）如无法执行测试，请明确说明原因，并至少保证项目可编译通过。
五、执行原则
1）如存在多种实现方式，请优先选择最小改动方案；
2）不得因实现便利而破坏既有工程结构或规范。
———————————————————————————————————————————————————————
【版本更新规范 V1】
以下规范用于约束版本号管理与 CHANGELOG.md 的维护方式，必须严格遵守：
一、CHANGELOG 文件规则
1）项目根目录下使用 CHANGELOG.md 作为唯一版本更新日志文件；
2）如果 CHANGELOG.md 不存在，必须新建；
3）CHANGELOG.md 仅用于记录版本更新，不承担其他说明职责。
二、版本记录新增规则
1）每次 Codex 完成一次有效修改任务后，必须更新 CHANGELOG.md；
2）新版本记录必须追加在文件顶部；
3）禁止修改、删除或重排历史版本记录。
三、版本号规则
1）版本号格式统一为：0.x.y；
2）x 表示功能版本号，y 表示修订版本号；
3）当本次修改属于新增功能或功能增强时：递增 x，y 重置为 0；
4）当本次修改属于问题修复、小改动或非功能性调整时：x 保持不变，仅递增 y；
5）版本号必须连续，不得跳号或回退。
四、版本记录内容规范
每个版本记录必须完整包含以下内容：
1）版本号（使用中括号包裹）；
2）修改日期，格式为 YYYY-MM-DD；
3）本次使用的完整 Codex 提示语/指令原文，必须原样写入，不得删减、概括或改写。
五、版本记录示例
# Changelog
## [版本号] - YYYY-MM-DD
### 本次 Codex 指令
```text
（完整提示语/指令原文）
———————————————————————————————————————————————————————
【UI组件与样式规范 V1】
以下规范用于约束本工程中所有 UI 的实现方式，基于 Material Design 3，必须严格遵守：
一、总体原则
1）所有 UI 必须基于 Material Design 3 组件体系实现；
2）优先使用 Material 3 提供的现有组件，而非自定义实现；
3）保持整体 UI 风格一致、克制、可维护。
二、组件使用规范
1）优先使用 Material 3 官方组件（如 Button、Text、Card、AlertDialog 等）；
2）禁止自行实现与 Material 3 已有组件功能重复的自定义组件；
3）如已有 Material 3 组件可满足需求，不得自定义替代；
4）避免使用过时或非 Material 3 的组件风格。
三、颜色与主题规范
1）所有颜色应来自 MaterialTheme.colorScheme；
2）禁止在业务代码中写死颜色值（如 Color(0xFFxxxxxx)）；
3）禁止新增非必要的颜色常量；
4）背景色、文字色应使用主题自动适配颜色（如 primary、onPrimary 等）。
四、字体与排版规范
1）文本样式应使用 MaterialTheme.typography 中的现有样式；
2）禁止在业务代码中自定义 TextStyle；
3）禁止手动指定字体大小、字体族或字重；
4）文本层级应通过选择合适的 typography 样式体现。
五、布局与间距规范
1）布局应以简单、清晰为原则；
2）避免过深的嵌套层级；
3）间距通过 Modifier.padding 实现；
4）禁止通过写死高度或宽度制造“视觉间距”。
六、状态与反馈规范
1）交互状态（enabled / disabled / loading 等）应使用 Material 3 组件自带能力；
2）避免自行实现状态视觉效果；
3）错误、提示等反馈优先使用 Material 3 组件（如 Snackbar、Dialog）。
七、风格一致性规范
1）同类页面应使用一致的组件与样式选择；
2）不得在不同页面中引入明显不同的 UI 风格；
3）如存在多种实现方式，应选择与现有页面风格最一致的方案。
八、工程实现约束
1）不得因 UI 实现需要而修改主题结构；
2）不得为 UI 目的引入新的依赖；
3）UI 实现应服务于业务清晰度，而非展示技巧。
```

## [0.4.1] - 2026-01-27
### 本次 Codex 指令
```text
下发Codex任务内容


【本次修改内容】

一、作用页面 / 模块：
- 底部导航栏中的“记录”按钮
- 记录按钮对应的记录页面（新增变化点页面）

二、问题现状说明：
1）当前底部导航栏中的“记录”按钮在未选中状态下，
   其视觉表现与其他导航按钮一致，缺乏明显的识别与强调；
2）记录页面存在上下滑动异常，页面内容无法正常向下滚动，
   影响完整表单内容的浏览。

三、期望行为：
1）为底部导航栏中的“记录”按钮增加一个固定的圆形背景区域：
   - 圆形背景区域位于“记录”按钮的外层；
   - 圆形背景区域始终存在，不随选中状态消失；
2）“记录”按钮的选中与未选中状态，仅通过圆形背景区域的颜色进行区分：
   - 选中状态使用正色；
   - 未选中状态使用反色；
   - 不通过尺寸变化、缩放、动画或阴影进行区分；
3）圆形背景区域仅用于强调“记录”按钮的特殊性，
   不改变按钮的点击行为与导航逻辑；
4）记录页面内容应支持正常的上下滑动，
   用户可以完整浏览当前页面中的所有表单内容。

四、边界与限制：
1）圆形背景区域的实现需基于 Material Design 3 的组件与样式体系；
2）不得改变底部导航栏的整体布局、排列方式或高度；
3）不得改变“记录”按钮及其他导航按钮的点击区域大小；
4）不得影响其他底部导航入口（总览、待办、追溯、用户）的显示或交互；
5）记录页面滑动问题的修复仅限于布局与滚动容器层面，
   不修改表单字段、校验逻辑或业务行为；
6）不得影响系统返回键及其他页面的返回逻辑。

五、修改类型：
- 问题修复


【需遵循规范】
一、通用执行规范 V1
二、Material Design 3 组件与样式规范 V1
三、版本更新规范 V1
———————————————————————————————————————————————————————
【通用执行规范 V1】
以下规范对本次及后续所有 Codex 修改任务生效，必须严格遵守：
一、工程边界
1）不要调整目录结构；
2）不要调整导航或页面结构；
3）不要重命名现有函数或变量；
4）不要移动文件；
5）不要引入新的第三方依赖；
6）不要影响其他页面的返回逻辑或既有行为。
二、修改范围
1）只允许修改本次任务明确指定的文件/页面/功能；
2）不要修改未明确允许的页面或模块；
3）如需新增状态，请使用页面内的最小本地状态；
4）不要擅自抽取公共组件或新增公共 UI 层。
三、输出要求
1）只输出最终代码，不要包含解释说明；
2）如修改文件，必须输出该文件的完整内容；
3）不要输出 diff 或片段代码。
四、质量与验证
1）修改后项目必须可以正常编译；
2）不允许新增编译错误或 warning；
3）修改完成后请执行一次构建验证（如 assembleDebug）；
4）不要口头声明“已测试”；
5）如无法执行测试，请明确说明原因，并至少保证项目可编译通过。
五、执行原则
1）如存在多种实现方式，请优先选择最小改动方案；
2）不得因实现便利而破坏既有工程结构或规范。
———————————————————————————————————————————————————————
【版本更新规范 V1】
以下规范用于约束版本号管理与 CHANGELOG.md 的维护方式，必须严格遵守：
一、CHANGELOG 文件规则
1）项目根目录下使用 CHANGELOG.md 作为唯一版本更新日志文件；
2）如果 CHANGELOG.md 不存在，必须新建；
3）CHANGELOG.md 仅用于记录版本更新，不承担其他说明职责。
二、版本记录新增规则
1）每次 Codex 完成一次有效修改任务后，必须更新 CHANGELOG.md；
2）新版本记录必须追加在文件顶部；
3）禁止修改、删除或重排历史版本记录。
三、版本号规则
1）版本号格式统一为：0.x.y；
2）x 表示功能版本号，y 表示修订版本号；
3）当本次修改属于新增功能或功能增强时：递增 x，y 重置为 0；
4）当本次修改属于问题修复、小改动或非功能性调整时：x 保持不变，仅递增 y；
5）版本号必须连续，不得跳号或回退。
四、版本记录内容规范
每个版本记录必须完整包含以下内容：
1）版本号（使用中括号包裹）；
2）修改日期，格式为 YYYY-MM-DD；
3）本次使用的完整 Codex 提示语/指令原文，必须原样写入，不得删减、概括或改写。
五、版本记录示例
# Changelog
## [版本号] - YYYY-MM-DD
### 本次 Codex 指令
```text
（完整提示语/指令原文）
———————————————————————————————————————————————————————
【UI组件与样式规范 V1】
以下规范用于约束本工程中所有 UI 的实现方式，基于 Material Design 3，必须严格遵守：
一、总体原则
1）所有 UI 必须基于 Material Design 3 组件体系实现；
2）优先使用 Material 3 提供的现有组件，而非自定义实现；
3）保持整体 UI 风格一致、克制、可维护。
二、组件使用规范
1）优先使用 Material 3 官方组件（如 Button、Text、Card、AlertDialog 等）；
2）禁止自行实现与 Material 3 已有组件功能重复的自定义组件；
3）如已有 Material 3 组件可满足需求，不得自定义替代；
4）避免使用过时或非 Material 3 的组件风格。
三、颜色与主题规范
1）所有颜色应来自 MaterialTheme.colorScheme；
2）禁止在业务代码中写死颜色值（如 Color(0xFFxxxxxx)）；
3）禁止新增非必要的颜色常量；
4）背景色、文字色应使用主题自动适配颜色（如 primary、onPrimary 等）。
四、字体与排版规范
1）文本样式应使用 MaterialTheme.typography 中的现有样式；
2）禁止在业务代码中自定义 TextStyle；
3）禁止手动指定字体大小、字体族或字重；
4）文本层级应通过选择合适的 typography 样式体现。
五、布局与间距规范
1）布局应以简单、清晰为原则；
2）避免过深的嵌套层级；
3）间距通过 Modifier.padding 实现；
4）禁止通过写死高度或宽度制造“视觉间距”。
六、状态与反馈规范
1）交互状态（enabled / disabled / loading 等）应使用 Material 3 组件自带能力；
2）避免自行实现状态视觉效果；
3）错误、提示等反馈优先使用 Material 3 组件（如 Snackbar、Dialog）。
七、风格一致性规范
1）同类页面应使用一致的组件与样式选择；
2）不得在不同页面中引入明显不同的 UI 风格；
3）如存在多种实现方式，应选择与现有页面风格最一致的方案。
八、工程实现约束
1）不得因 UI 实现需要而修改主题结构；
2）不得为 UI 目的引入新的依赖；
3）UI 实现应服务于业务清晰度，而非展示技巧。
```

## [0.4.0] - 2026-01-27
### 本次 Codex 指令
```text
【本次修改内容】

一、作用页面 / 模块 / 功能：
- 底部导航栏中的“发起”入口
- 发起入口对应的页面流程
- 新增变化点页面（界面 2）

二、现状说明：
1）当前底部导航栏中存在“发起”按钮；
2）点击“发起”按钮后，先进入发起入口页（界面 1）；
3）在界面 1 中点击“新建变化点”按钮后，才进入新增变化点页面（界面 2）。

三、期望行为：
1）将底部导航栏中的“发起”入口名称修改为“记录”；
2）用户点击底部导航栏中的“记录”按钮后：
   - 不再进入发起入口页（界面 1）；
   - 直接进入新增变化点页面（界面 2）。
3）新增变化点页面的页面结构调整为：
   - 页面顶部显示固定标题：“记录变更”；
   - 页面中间区域显示当前新增变化点表单内容（保持现有表单结构与字段不变）；
   - 页面底部显示一个固定操作按钮：“取消记录”。

四、按钮与样式要求：
1）底部导航栏中的“记录”按钮需要在导航栏中居中显示；
2）“记录”按钮需采用与其他导航按钮不同的颜色样式，以形成视觉上的凸显；
3）除上述差异外，其余导航按钮样式保持不变。

五、边界与限制：
1）不新增新的业务流程，仅调整现有页面跳转路径；
2）不修改新增变化点表单的字段、校验逻辑或提交行为；
3）发起入口页（原界面 1）不再作为用户可达页面，但不要求删除其代码；
4）不影响其他底部导航入口（总览、待办、追溯、用户）的行为；
5）不影响系统返回键及其他页面的返回逻辑。

六、修改类型：
- 功能增强


【需遵循规范】
一、通用执行规范 V1
二、Material Design 3 组件与样式规范 V1
三、版本更新规范 V1
———————————————————————————————————————————————————————
【通用执行规范 V1】
以下规范对本次及后续所有 Codex 修改任务生效，必须严格遵守：
一、工程边界
1）不要调整目录结构；
2）不要调整导航或页面结构；
3）不要重命名现有函数或变量；
4）不要移动文件；
5）不要引入新的第三方依赖；
6）不要影响其他页面的返回逻辑或既有行为。
二、修改范围
1）只允许修改本次任务明确指定的文件/页面/功能；
2）不要修改未明确允许的页面或模块；
3）如需新增状态，请使用页面内的最小本地状态；
4）不要擅自抽取公共组件或新增公共 UI 层。
三、输出要求
1）只输出最终代码，不要包含解释说明；
2）如修改文件，必须输出该文件的完整内容；
3）不要输出 diff 或片段代码。
四、质量与验证
1）修改后项目必须可以正常编译；
2）不允许新增编译错误或 warning；
3）修改完成后请执行一次构建验证（如 assembleDebug）；
4）不要口头声明“已测试”；
5）如无法执行测试，请明确说明原因，并至少保证项目可编译通过。
五、执行原则
1）如存在多种实现方式，请优先选择最小改动方案；
2）不得因实现便利而破坏既有工程结构或规范。
———————————————————————————————————————————————————————
【版本更新规范 V1】
以下规范用于约束版本号管理与 CHANGELOG.md 的维护方式，必须严格遵守：
一、CHANGELOG 文件规则
1）项目根目录下使用 CHANGELOG.md 作为唯一版本更新日志文件；
2）如果 CHANGELOG.md 不存在，必须新建；
3）CHANGELOG.md 仅用于记录版本更新，不承担其他说明职责。
二、版本记录新增规则
1）每次 Codex 完成一次有效修改任务后，必须更新 CHANGELOG.md；
2）新版本记录必须追加在文件顶部；
3）禁止修改、删除或重排历史版本记录。
三、版本号规则
1）版本号格式统一为：0.x.y；
2）x 表示功能版本号，y 表示修订版本号；
3）当本次修改属于新增功能或功能增强时：递增 x，y 重置为 0；
4）当本次修改属于问题修复、小改动或非功能性调整时：x 保持不变，仅递增 y；
5）版本号必须连续，不得跳号或回退。
四、版本记录内容规范
每个版本记录必须完整包含以下内容：
1）版本号（使用中括号包裹）；
2）修改日期，格式为 YYYY-MM-DD；
3）本次使用的完整 Codex 提示语/指令原文，必须原样写入，不得删减、概括或改写。
五、版本记录示例
# Changelog
## [版本号] - YYYY-MM-DD
### 本次 Codex 指令
```text
（完整提示语/指令原文）
———————————————————————————————————————————————————————
【UI组件与样式规范 V1】
以下规范用于约束本工程中所有 UI 的实现方式，基于 Material Design 3，必须严格遵守：
一、总体原则
1）所有 UI 必须基于 Material Design 3 组件体系实现；
2）优先使用 Material 3 提供的现有组件，而非自定义实现；
3）保持整体 UI 风格一致、克制、可维护。
二、组件使用规范
1）优先使用 Material 3 官方组件（如 Button、Text、Card、AlertDialog 等）；
2）禁止自行实现与 Material 3 已有组件功能重复的自定义组件；
3）如已有 Material 3 组件可满足需求，不得自定义替代；
4）避免使用过时或非 Material 3 的组件风格。
三、颜色与主题规范
1）所有颜色应来自 MaterialTheme.colorScheme；
2）禁止在业务代码中写死颜色值（如 Color(0xFFxxxxxx)）；
3）禁止新增非必要的颜色常量；
4）背景色、文字色应使用主题自动适配颜色（如 primary、onPrimary 等）。
四、字体与排版规范
1）文本样式应使用 MaterialTheme.typography 中的现有样式；
2）禁止在业务代码中自定义 TextStyle；
3）禁止手动指定字体大小、字体族或字重；
4）文本层级应通过选择合适的 typography 样式体现。
五、布局与间距规范
1）布局应以简单、清晰为原则；
2）避免过深的嵌套层级；
3）间距通过 Modifier.padding 实现；
4）禁止通过写死高度或宽度制造“视觉间距”。
六、状态与反馈规范
1）交互状态（enabled / disabled / loading 等）应使用 Material 3 组件自带能力；
2）避免自行实现状态视觉效果；
3）错误、提示等反馈优先使用 Material 3 组件（如 Snackbar、Dialog）。
七、风格一致性规范
1）同类页面应使用一致的组件与样式选择；
2）不得在不同页面中引入明显不同的 UI 风格；
3）如存在多种实现方式，应选择与现有页面风格最一致的方案。
八、工程实现约束
1）不得因 UI 实现需要而修改主题结构；
2）不得为 UI 目的引入新的依赖；
3）UI 实现应服务于业务清晰度，而非展示技巧。

<image></image>
<image></image>
```

## [0.3.1] - 2026-01-27
### 变更说明
- 修复登录页面确认退出后未正确退出应用的问题

### 本次 Codex 指令
```text
下发Codex任务内容


【本次修改内容】

一、作用页面 / 模块 / 功能：
登录页面

二、触发条件：
用户在登录页面点击现有“退出”按钮，并在当前已有的确认流程中选择“确认”。

三、期望行为：
系统应正确执行退出应用的逻辑，应用退出并回到系统桌面。

四、边界与限制：
本次修改不新增任何确认对话框或新的交互流程，仅修复“确认后未正确退出应用”的问题；
退出逻辑应复用现有实现方式，不新增全局或替代性的退出方案，不得影响系统返回键或其他页面的退出行为。

五、修改类型：
本次修改类型：新增功能/功能增强/问题修复/小改动/非功能性调整，请按版本更新规范执行版本号递增规则。


【需遵循规范】
一、通用执行规范 V1
二、Material Design 3 组件与样式规范 V1
三、版本更新规范 V1
———————————————————————————————————————————————————————
【通用执行规范 V1】
以下规范对本次及后续所有 Codex 修改任务生效，必须严格遵守：
一、工程边界
1）不要调整目录结构；
2）不要调整导航或页面结构；
3）不要重命名现有函数或变量；
4）不要移动文件；
5）不要引入新的第三方依赖；
6）不要影响其他页面的返回逻辑或既有行为。
二、修改范围
1）只允许修改本次任务明确指定的文件/页面/功能；
2）不要修改未明确允许的页面或模块；
3）如需新增状态，请使用页面内的最小本地状态；
4）不要擅自抽取公共组件或新增公共 UI 层。
三、输出要求
1）只输出最终代码，不要包含解释说明；
2）如修改文件，必须输出该文件的完整内容；
3）不要输出 diff 或片段代码。
四、质量与验证
1）修改后项目必须可以正常编译；
2）不允许新增编译错误或 warning；
3）修改完成后请执行一次构建验证（如 assembleDebug）；
4）不要口头声明“已测试”；
5）如无法执行测试，请明确说明原因，并至少保证项目可编译通过。
五、执行原则
1）如存在多种实现方式，请优先选择最小改动方案；
2）不得因实现便利而破坏既有工程结构或规范。
———————————————————————————————————————————————————————
【版本更新规范 V1】
以下规范用于约束版本号管理与 CHANGELOG.md 的维护方式，必须严格遵守：
一、CHANGELOG 文件规则
1）项目根目录下使用 CHANGELOG.md 作为唯一版本更新日志文件；
2）如果 CHANGELOG.md 不存在，必须新建；
3）CHANGELOG.md 仅用于记录版本更新，不承担其他说明职责。
二、版本记录新增规则
1）每次 Codex 完成一次有效修改任务后，必须更新 CHANGELOG.md；
2）新版本记录必须追加在文件顶部；
3）禁止修改、删除或重排历史版本记录。
三、版本号规则
1）版本号格式统一为：0.x.y；
2）x 表示功能版本号，y 表示修订版本号；
3）当本次修改属于新增功能或功能增强时：递增 x，y 重置为 0；
4）当本次修改属于问题修复、小改动或非功能性调整时：x 保持不变，仅递增 y；
5）版本号必须连续，不得跳号或回退。
四、版本记录内容规范
每个版本记录必须完整包含以下内容：
1）版本号（使用中括号包裹）；
2）修改日期，格式为 YYYY-MM-DD；
3）本次使用的完整 Codex 提示语/指令原文，必须原样写入，不得删减、概括或改写。
五、版本记录示例
# Changelog
## [版本号] - YYYY-MM-DD
### 本次 Codex 指令
```text
（完整提示语/指令原文）
———————————————————————————————————————————————————————
【UI组件与样式规范 V1】
以下规范用于约束本工程中所有 UI 的实现方式，基于 Material Design 3，必须严格遵守：
一、总体原则
1）所有 UI 必须基于 Material Design 3 组件体系实现；
2）优先使用 Material 3 提供的现有组件，而非自定义实现；
3）保持整体 UI 风格一致、克制、可维护。
二、组件使用规范
1）优先使用 Material 3 官方组件（如 Button、Text、Card、AlertDialog 等）；
2）禁止自行实现与 Material 3 已有组件功能重复的自定义组件；
3）如已有 Material 3 组件可满足需求，不得自定义替代；
4）避免使用过时或非 Material 3 的组件风格。
三、颜色与主题规范
1）所有颜色应来自 MaterialTheme.colorScheme；
2）禁止在业务代码中写死颜色值（如 Color(0xFFxxxxxx)）；
3）禁止新增非必要的颜色常量；
4）背景色、文字色应使用主题自动适配颜色（如 primary、onPrimary 等）。
四、字体与排版规范
1）文本样式应使用 MaterialTheme.typography 中的现有样式；
2）禁止在业务代码中自定义 TextStyle；
3）禁止手动指定字体大小、字体族或字重；
4）文本层级应通过选择合适的 typography 样式体现。
五、布局与间距规范
1）布局应以简单、清晰为原则；
2）避免过深的嵌套层级；
3）间距通过 Modifier.padding 实现；
4）禁止通过写死高度或宽度制造“视觉间距”。
六、状态与反馈规范
1）交互状态（enabled / disabled / loading 等）应使用 Material 3 组件自带能力；
2）避免自行实现状态视觉效果；
3）错误、提示等反馈优先使用 Material 3 组件（如 Snackbar、Dialog）。
七、风格一致性规范
1）同类页面应使用一致的组件与样式选择；
2）不得在不同页面中引入明显不同的 UI 风格；
3）如存在多种实现方式，应选择与现有页面风格最一致的方案。
八、工程实现约束
1）不得因 UI 实现需要而修改主题结构；
2）不得为 UI 目的引入新的依赖；
3）UI 实现应服务于业务清晰度，而非展示技巧。
```

## [0.3.0] - 2026-01-27
### 变更说明
- 总览、待办、发起、追溯、用户页面增加标题区域

### 本次 Codex 指令
````text
本次修改内容：

分别在以下页面中增加一个标题区域，用于显示当前页面名称：
- 总览页面
- 待办页面
- 发起页面
- 追溯页面
- 用户页面

页面标题区域 UI 设计规范（V1）如下（必须严格遵循）：

1）位置规范：
- 标题区域位于页面内容最顶部；
- 在页面根布局内直接声明；
- 不使用 TopAppBar，不使用 Scaffold 的 topBar；
- 标题区域随页面内容滚动，不悬浮、不覆盖内容。

2）尺寸规范：
- 标题区域高度随内容自适应；
- 不写死固定高度；
- 通过内边距形成区域感，不通过高度控制。

3）背景规范：
- 背景色使用蓝色；
- 优先使用 MaterialTheme.colorScheme.primary 或项目中已有主题色；
- 不新增颜色常量，不写死非主题色值。

4）文本规范：
- 仅显示当前页面名称文本；
- 不显示副标题、不显示图标；
- 使用 Material Design 3 默认文本样式；
- 不自定义 TextStyle，不新增字体或字号定义；
- 文本颜色优先使用主题自动适配（如 onPrimary）。

5）行为规范：
- 无任何交互行为；
- 不可点击、不响应手势；
- 不绑定业务状态。

6）工程实现规范：
- 不引入 ViewModel 状态；
- 不抽取公共组件；
- 每个页面独立实现，允许少量重复代码；
- 优先最小侵入、最小改动方案。

通用约束：
- 只允许修改各页面对应的文件；
- 不要修改其他页面；
- 不要改变现有 UI 布局和登录流程；
- 不要引入新的依赖；
- 不要影响其他页面的返回逻辑；
- 如需新增状态，请使用页面内的最小本地状态。

输出要求：
- 不要调整目录结构；
- 不要调整导航或页面结构；
- 不要重命名现有函数或变量；
- 不要移动文件；
- 只输出最终代码，不要包含解释说明；
- 保持 Material Design 3 的组件和样式不变；
- 修改后不能引入新的警告或编译错误；
- 如果不确定，请先选择最小改动方案；
- 修改完成后，请执行一次构建验证，确保项目可以正常编译；
- 不要口头声明“已测试”。如果无法执行测试，
  请明确说明原因，并至少保证项目可编译通过。

附加要求（必须执行）：
1）本次修改完成后，请更新 CHANGELOG.md：
   - 如果文件不存在，请新建；
   - 在文件顶部新增一个版本记录。
2）版本号规则：
   - 使用格式：0.x.y；
   - 本次属于新增功能，请递增 x，y 归零。
3）每个版本记录需包含以下内容：
   - 版本号
   - 修改日期（YYYY-MM-DD）
   - 本次修改说明（简要）
   - 本次使用的完整提示语/指令原文（原样写入）
4）CHANGELOG.md 只允许追加内容，不要修改历史版本记录。
5）除 CHANGELOG.md 和本次明确允许修改的页面文件外，
   不要修改其他文件。
6）只输出最终修改后的完整文件内容（包括 CHANGELOG.md）。
7）示例如下：
# Changelog
## [0.1.1] - 2026-01-27
### 本次 Codex 指令
XXXXXX
...
````

## [0.2.0] - 2026-01-27
### 变更说明
- 用户页面增加账号登出二次确认对话框

### 本次 Codex 指令
````text
# AGENTS.md instructions for /workspace/MyFirstComposeApp

<INSTRUCTIONS>
## Skills
A skill is a set of local instructions to follow that is stored in a `SKILL.md` file. Below is the list of skills that can be used. Each entry includes a name, description, and file path so you can open the source for full instructions when using a specific skill.
### Available skills
- skill-creator: Guide for creating effective skills. This skill should be used when users want to create a new skill (or update an existing skill) that extends Codex's capabilities with specialized knowledge, workflows, or tool integrations. (file: /opt/codex/skills/.system/skill-creator/SKILL.md)
- skill-installer: Install Codex skills into $CODEX_HOME/skills from a curated list or a GitHub repo path. Use when a user asks to list installable skills, install a curated skill, or install a skill from another repo (including private repos). (file: /opt/codex/skills/.system/skill-installer/SKILL.md)
### How to use skills
- Discovery: The list above is the skills available in this session (name + description + file path). Skill bodies live on disk at the listed paths.
- Trigger rules: If the user names a skill (with `$SkillName` or plain text) OR the task clearly matches a skill's description shown above, you must use that skill for that turn. Multiple mentions mean use them all. Do not carry skills across turns unless re-mentioned.
- Missing/blocked: If a named skill isn't in the list or the path can't be read, say so briefly and continue with the best fallback.
- How to use a skill (progressive disclosure):
  1) After deciding to use a skill, open its `SKILL.md`. Read only enough to follow the workflow.
  2) If `SKILL.md` points to extra folders such as `references/`, load only the specific files needed for the request; don't bulk-load everything.
  3) If `scripts/` exist, prefer running or patching them instead of retyping large code blocks.
  4) If `assets/` or templates exist, reuse them instead of recreating from scratch.
- Coordination and sequencing:
  - If multiple skills apply, choose the minimal set that covers the request and state the order you'll use them.
  - Announce which skill(s) you're using and why (one short line). If you skip an obvious skill, say why.
- Context hygiene:
  - Keep context small: summarize long sections instead of pasting them; only load extra files when needed.
  - Avoid deep reference-chasing: prefer opening only files directly linked from `SKILL.md` unless you're blocked.
  - When variants exist (frameworks, providers, domains), pick only the relevant reference file(s) and note that choice.
- Safety and fallback: If a skill can't be applied cleanly (missing files, unclear instructions), state the issue, pick the next-best approach, and continue.
</INSTRUCTIONS>

<environment_context>
  <cwd>/workspace/MyFirstComposeApp</cwd>
  <shell>bash</shell>
</environment_context>

在用户页面中，当用户点击“账号登出”按钮时，弹出一个二次确认对话框（使用 Material 3 的 AlertDialog）。

对话框文案
标题：提示  
内容：确定要退出当前用户登录吗？  
按钮：确认 / 取消  

行为要求：
-点击确认才执行现有退出逻辑；
-点击取消仅关闭对话框，不做任何操作。

约束：
-只允许修改用户页面对应的文件；
-不要修改其他页面；
-不要改变现有 UI 布局和登录流程；
-不要引入新的依赖；
-不要影响其他页面的返回逻辑；
-如需新增状态，请使用页面内的最小本地状态。

输出要求
-不要调整目录结构；
-不要调整导航或页面结构；
-不要重命名现有函数或变量；
-不要移动文件；
-只输出最终代码，不要包含解释说明；
-保持 Material Design 3 的组件和样式不变；
-修改后不能引入新的警告或编译错误；
-如果不确定，请先选择最小改动方案。
-修改完成后，请执行一次构建验证：确保项目可以正常编译（不允许新增编译错误或警告）。如果发现编译失败，请修复后再输出最终代码。
-不要口头声明“已测试”。如果无法执行测试，请明确说明无法执行的原因，并至少保证项目可编译通过。

附加要求（必须执行）：
1）本次修改完成后，请更新 CHANGELOG.md：
- 如果文件不存在，请新建。
- 在文件顶部新增一个版本记录。
2）版本号规则：
- 使用格式：0.x.y
- 如果是新增功能，递增 x，y 归零；
- 如果是修复或小改动，仅递增 y。
3）每个版本记录需包含以下内容：
- 版本号
- 修改日期（YYYY-MM-DD）
- 本次修改说明（简要）
- 本次使用的完整提示语/指令原文（原样写入）
4）CHANGELOG.md 只允许追加内容，不要修改历史版本记录。
5）除 CHANGELOG.md 和本次明确允许修改的代码文件外，
不要修改其他文件。
6）只输出最终修改后的完整文件内容（包括 CHANGELOG.md）。
7）示例如下：
# Changelog

## [0.1.1] - 2026-01-27
### 变更说明
- 登录页面增加退出二次确认对话框

### 本次 Codex 指令
```text
在 LoginScreen.kt 中，
当用户点击“退出”按钮时，弹出一个二次确认对话框。
...
```
````

## [0.1.0] - 2026-01-27
### 变更说明
- 登录页面增加退出二次确认对话框

### 本次 Codex 指令
````text
在登录页面中，当用户点击“退出”按钮时，弹出一个二次确认对话框（使用 Material 3 的 AlertDialog）。

对话框文案
标题：提示  
内容：确定要退出应用吗？  
按钮：确认 / 取消  

行为要求：
-点击确认才执行现有退出逻辑；
-点击取消仅关闭对话框，不做任何操作。

约束：
-只允许修改登录页面对应的文件；
-不要修改其他页面；
-不要改变现有 UI 布局和登录流程；
-不要引入新的依赖；
-不要影响其他页面的返回逻辑；
-如需新增状态，请使用页面内的最小本地状态。

输出要求
-不要调整目录结构；
-不要调整导航或页面结构；
-不要重命名现有函数或变量；
-不要移动文件；
-只输出最终代码，不要包含解释说明；
-保持 Material Design 3 的组件和样式不变；
-修改后不能引入新的警告或编译错误；
-如果不确定，请先选择最小改动方案。


附加要求（必须执行）：
1）本次修改完成后，请更新 CHANGELOG.md：
- 如果文件不存在，请新建。
- 在文件顶部新增一个版本记录。
2）版本号规则：
- 使用格式：0.x.y
- 如果是新增功能，递增 x，y 归零；
- 如果是修复或小改动，仅递增 y。
3）每个版本记录需包含以下内容：
- 版本号
- 修改日期（YYYY-MM-DD）
- 本次修改说明（简要）
- 本次使用的完整提示语/指令原文（原样写入）
4）CHANGELOG.md 只允许追加内容，不要修改历史版本记录。
5）除 CHANGELOG.md 和本次明确允许修改的代码文件外，
不要修改其他文件。
6）只输出最终修改后的完整文件内容（包括 CHANGELOG.md）。
7）示例如下：
# Changelog

## [0.1.1] - 2026-01-27
### 变更说明
- 登录页面增加退出二次确认对话框

### 本次 Codex 指令
```text
在 LoginScreen.kt 中，
当用户点击“退出”按钮时，弹出一个二次确认对话框。
...
```
````
